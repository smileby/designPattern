Template Method，模板方法：

定义一个操作中的算法的骨架，而将一些步骤延迟到子类中，
TemplateMethod使得子类可以不改变一个算法的结构即可以重定义该算法得某些特定步骤。 


案例：
	SpringJDBC实现了模板方法模式：
	// 实现了java规范，具体实现由各个数据库厂商进行实现
	// 加载驱动
	// 建立连接
	// 创建语句集（标准语句集，预处理语句集）
	// 执行语句集
	// 返回结果集ResultSet