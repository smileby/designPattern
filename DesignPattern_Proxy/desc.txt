Proxy，代理模式：为其他对象提供一种代理以控制对这个对象的访问

1、静态代理
	实现方式：
		1、定义被代理方法接口
		2、被代理类实现接口，填充指定业务逻辑
		3、定义proxy类
			① 定义私有的被代理类接口属性
			② 定义构造器，有参和无参两种，一种直接new被代理类、一种使用传入的方式
			③ 提供public的方法，对外暴露
			④ 外部调用，指定代理类方法

2、动态代理

